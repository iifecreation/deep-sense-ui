name: Tenant UI quality

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint:baseline
      - run: npm run build
      - run: npx playwright install --with-deps
      - name: Supported browser API contracts
        run: npx playwright test e2e/api-contracts.spec.ts
        env:
          PLAYWRIGHT_SKIP_WEBSERVER: "1"
