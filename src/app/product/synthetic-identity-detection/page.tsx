"use client";

import React from "react";
import CategoryPageTemplate from "@/components/common/CategoryPageTemplate";

export default function SyntheticIdentityDetectionPage() {
  return (
    <CategoryPageTemplate 
      category="Product"
      title="Synthetic Identity Detection"
      description="Identify synthesized profiles built from fake or manipulated attributes."
      ctaText="Stop Synthetic Fraud"
    />
  );
}
